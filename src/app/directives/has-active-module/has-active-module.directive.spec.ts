import { HasActiveModuleDirective } from "./has-active-module.directive";

describe('HasActiveModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new HasActiveModuleDirective();
    expect(directive).toBeTruthy();
  });
});

import { TableResponsiveDirective } from './table-responsive.directive';

describe('TableResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TableResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
